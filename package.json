{
  "name": "json-task-repo",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.js",
    "test": "npm run validate"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
